.text     → program code (flash)
.rodata   → constants (flash)
.data     → initialized globals (RAM, copied from flash)
.bss      → uninitialized globals (RAM)
.stack    → grows downward
.heap     → grows upward